package com.example.kpkn.ui.components

import androidx.compose.ui.graphics.Color

/**
 * Kept as a compatibility helper for older surfaces. Canonical anatomy is
 * intentionally neutral now; semantic role and volume carry the hierarchy,
 * never a rainbow of muscle-specific accents.
 */
fun canonicalMuscleColor(name: String): Color = Color(0xFF8B919A)

val CONCEPTS_DIVIDER: Color = Color(0xFF2A2A2A)
val CONCEPTS_LINK_COLOR: Color = Color(0xFF9DB6C9)
