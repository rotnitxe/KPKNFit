The user prefers a specialized KAUPOLIKAN agent that preserves context between sessions, minimizes exploratory token usage, and explains risky changes before applying them.

After each request, reinstall and relaunch the Android app on the open emulator (`installBaseDebug` + force-stop + start `com.example.kpkn/.MainActivity` on the `device` emulator, usually `emulator-5554`).
