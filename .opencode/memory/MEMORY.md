KPKN is local-first and private; Room is the source of truth on-device.
§
Voice-mode fixes sesión 2026-08-07 (equipo 4 agentes, informes en .opencode/team-reports/task_0001-*.md a 0007): issue 3 (rings 99%/no guarda) fue mayormente falso positivo — persistía; faltaban sets por issue 1. Ola 1 carga-clarificación: WorkoutContinuousVoiceEngine guardia anti-eco post-TTS ahora scoped por pendingClarificationActive; WorkoutVoiceController gracia stale-final, sin anuncios anidados bajo pendingAction, miss-counting/accumulator reset. Ola 2 último-descanso: gate de parse feedback admite voiceFeedbackPromptActive + fallback IntentMatcher OR; final-rest habla vía orchestrator; número suelto "1-10"=calidad; matcher molestias ponderado+sinónimos+drill-down por zonas; labels desde catálogo discomfortLabel(id); timer rest con shrink animado (WorkoutRestOverlay). Ola 3 persistencia P0: saveKeywords incl "sesión terminada"→isSaveAction; handleVoiceLogFinalFeedback llama ports.finalizeVoiceSession() directo (save desacoplado de Compose, WorkoutVoiceCommandHandler.kt:836-921) con guards doble-disparo; TTS "guardado" solo post-write en onComplete + onFailure hablado (WorkoutViewModel.kt:2800-2845); guard sesión vacía voz+UI (handler:842, FinishController:132, handleEmptySessionFinishBlocked); fallback hablado para frase final vacía (handler:913). Validado: compileBaseDebugKotlin + testBaseDebugUnitTest filtrado BUILD SUCCESSFUL. Resta P1: reactivación de capture no contemplada; paridad iOS de cambios de parser pendiente.
§
The Android product is under android-native/ and uses Kotlin, Compose, Clean Architecture, MVVM, manual DI, and StateFlow.
§
AUGE changes must preserve behavior across Android, iOS, and backend implementations.
§
The voice subsystem under android-native/app/src/main/java/com/example/kpkn/services/workout/ is large and active; use focused tests and diagnostics before broad refactors.
§
Android module has product flavors base/health; targeted tasks are compileBaseDebugKotlin and testBaseDebugUnitTest (bare compileDebugKotlin is ambiguous; assembleDebug builds both flavors).
§
Catalog V2 picker perf (2026-06): process cache in data/exercises/catalogv2/CatalogV2ProcessCache.kt (decodes exercise_catalog_v2.json once per process, prefetched in MainActivity step 9); ExercisePickerV2Catalog search is debounced 150ms on Dispatchers.Default via produceState; card compatibility/exactInfo/volume contributions are remember-memoized.
§
NutriTelemetry (2026-06, telemetry/nutrition/*): JSONL on-device exclusivo de nutrición en filesDir/nutrition_telemetry/ (rotación 512KB, retención 30d/10MB/24 archivos), sanitizer sin texto de comidas ni secretos, trazas por análisis con spans de etapa, marcadores in-flight + crash hook en proceso principal (KpknApplication), export SAF en Ajustes > Datos y app, análisis con android-native/scripts/analyze_nutrition_logs.py. Desactivable vía prefs flag "nutrition_telemetry/telemetry_enabled".
§
Crash al pulsar "Analizar" (FoodLoggerDrawer): causa raíz probable = regex no posesivo en FoodTemplateMatcher.QUANTITY_ANCHOR_PATTERN (backtracking exponencial → StackOverflowError en main); fix = cuantificadores posesivos + entrada acotada a 600 chars. Además: pipeline/Templates ahora corren dentro de la coroutine con try/Throwable (menos CancellationException), fallback con try anidado, CoroutineExceptionHandler en el scope del drawer, y !! de estado Compose reemplazados por lecturas seguras (comentarios CRASH-FIX).
§
The working tree may contain untracked WIP; check git status before blaming your own edits for build errors. (Nota previa resuelta: telemetry/nutrition/* ya compila y está integrado — ver entradas anteriores.)
§
Catalog V2 picker perf measured on desktop JVM (JDK17): decode 2.76MB asset ~34ms + index ~90ms per open (old path), search ~9.2ms/call on main thread per keystroke (old), compatibility ~5µs/call. On mid-range device expect ~3-5x: ~0.4-0.8s per picker open and ~30-50ms main-thread per keystroke before the fix; ~0 after.
§
SessionEditor drag&drop errático (2026-08-06): causa raíz = boundsInWindow congelados + LazyColumn virtualización + sin auto-scroll. Fix en screens/sessioneditor/*: DragController con frozen* en window compensado por applyScrollDelta(-delta) + registro de items virtualizados que entran por auto-scroll (registerExerciseBoundsDuringDrag/registerPartBoundsDuringDrag) + mergeBounds para zonas; Screen con LazyListState auto-scroll a 96dp threshold @18dp/16ms via listState.scroll{scrollBy} + captura de viewport window bounds; GroupEditorCard translationY corregido. Validado con compileBaseDebugKotlin + testBaseDebugUnitTest + assembleBaseDebug.
§
SessionEditor rendimiento (2026-08-07): auditoría 1 (C1-C4,A1-A5,M1-M10,B1-B4). Fixes: A1 exerciseIndex O(1) via catalogExerciseIndex(); M1 groupedParts remember(session.parts); A5 exactMatch remember(definitionsById); B1 contentType {it::class} en items; C3 commit on-finish en EditorMiniField/AccentSetValueField (onDone/focusLost) + slider onValueChangeFinished + updateSession guard early-return sin lastModifiedAtMs + dirty flag; A4 key estructural remember(session.parts,exercises,supersetGroups) + allExercisesForUi + textField sin scheduleAugeRecalc; M6 updateExerciseDrag con groupedParts param; C4 weeklyMetricsCache por contentHash (excluye name/description/lastModified) + catalogVersion/settingsHash + perSession map + buildAugeSummaryFromMetrics + assistant bajo demanda (inmediato si AUGE abierto else debounce 2500ms + refreshAssistantImmediate en openSheet); M2/M4 supersetGroupsById memo + anchorNames. Validado compileBaseDebugKotlin BUILD SUCCESSFUL (2-3m), sin tocar domain/auge fórmulas, Room v20 autoridad, voz no tocada.
§
Supersets opcionales (2026-08-08): auditoría 2 (C1 isOptional borrado, C2 rounds<sets). Fixes: C1 SupersetRules.normalizeSession propaga isOptional (degradación segura, vivo ignora pero preserva); C2 updateRest/createSuperset clamp rounds >= max(sets) evita sets inalcanzables y progreso <100%; A3 vivo cap 4 honesto (WorkoutStructureSheetsHost take(4) + WorkoutVoiceCommandHandler cappedNames con mensaje recorte); A4 dissolve preserva restTime original (restTime ?: copiedRoundRest); M6 moveSupersetGroupToIndex deprecado (orden vivo por posición primer miembro). Validado compileBaseDebugKotlin BUILD SUCCESSFUL.
§
Unilateral (2026-08-09): auditoría 3 F0-F1 hotfix. Fixes: #1 SetExecutionCard sideKey Triple(id,idx,side) evita contaminación drops/rest-pause L→R; #2 ExerciseEditorCard preserva restBetweenSidesSeconds en superset (no borra); #4 WorkoutStepNavigator expectedSidesForSet y skip single-side sin fantasma (L solo); #5 isSetDone single-side via UnilateralRules.expectedSidesForSet (Session.kt helper + WorkoutViewModel/WorkoutEditingRules), centralizado en domain/workout/UnilateralRules.kt; #12 dedup isEffectivelyUnilateral. Validado compileBaseDebugKotlin BUILD SUCCESSFUL.
§
Reglas y tiempo (2026-08-07): auditoría 4 C1-C2/D4/D8/D1-D3/#6/#3-#4-#11. Fixes: C1 F0 patch/updateRuleDefaults/updateRuleLimits persistDraft inmediato (SharedPreferences) sin upsert/lastModified; C2/D4 F1 seed mediana restTime/side/restSuperset/sets/reps/RPE desde draft.allExercises() si no hay persistedDraft; RulesEngine #2 F1 preserve restBetweenSidesSeconds (0→keep existing) + D8 scope cross-part all-check; D1/D2 F2 Calculations calculateSessionTimeBreakdown param restTimerDefaultSeconds + betweenSides *sets + roundRest per-round + AugeComputation/settings.default + ViewModel recalc+TimeCoach con settings; #6 F2 WorkoutSetRecorder WARMUP warmupSets.restBetween + density roundRestAfter + restTime settings; #3/#11 F3 @Deprecated Exercise.timeStrategy + ExerciseSet.restBetweenSides + SessionEditorRuleLimits legacy. Validado compileBaseDebugKotlin BUILD SUCCESSFUL 3m42s + testBaseDebugUnitTest SessionEditorRulesEngineTest BUILD SUCCESSFUL.
§
Asistente y barrido (2026-08-07): auditoría 5 A1-A6/B1-B8. Fixes: A1 F0 borra SessionEditorDebugLog.kt + 3 debug-9ba5f2.log trackeados + gut AssistantSheet/HistorySheet/RulesSheet H-A/H-C/H-E + SessionEditorAuditDebugTest sin logs + .gitignore; A3 F1 quita onApplyAugeCorrection/onAddGhostExercise de AssistantSheet/GlassOverlay + Screen/Sheets dead import; B2 F1 single-shot applyTemplateInternal return tras launch; B1 F1 variantes deepClone via SessionTemplateEngine.cloneSessionContent + Screen activeVariantSession + ViewModel updateSession variant-aware + updateActiveVariantSession helper + recalcAuge activeVariant; A4b F2 step 2 non-overlap + AugeActions skip si ya agrupado; A5 F2 PacingController dedupe L+R distinct; B6 F2 REPLACE limpia sessionB/C/D + backup/competition; B7 F2 mesociclo deepClone con cloneSessionContent inner ids; B4 F2 persistRecoverableSession en IO (viewModelScope.launch + runBlocking IO) para requestSessionSwitch/createSessionForDay; B5 F2 borra appendDraftSnapshot/buildDraftSnapshot + CoverClone deprecated wrappers; B8 F3 toggleSelection persistDraft + setTargetDuration variant-aware. Validado compileBaseDebugKotlin BUILD SUCCESSFUL (2m31s+51s).
§
Plan aprobado pendiente de pipeline (2026-08-18): "Circuitos de cardio programables + HIIT" en .opencode/plans/2026-08-18_cardio-intervalos-hiit.md — flags: [auge]. El usuario aprobó verbalmente y commiteó el WIP cardio previo (97ea5270). BLOQUEO: pipeline.json sigue en stage "construction" del plan 2026-08-17_auge-ring-drainage-audit y solo la tool `pipeline` puede reasignarlo (no estaba expuesta en la sesión del orquestador). Antes de construir hay que registrar start/request_approval/approve apuntando al plan nuevo. Claves del diseño: intervalBlocks+intervalRounds en CardioDetails con defaults (sin migración Room, v23), enum CardioBlockType NUEVO (nunca valores nuevos en CardioType/CardioIntensity — dbJson sin coerceInputValues), bloques como sub-estado derivado de CardioTimerState (sin tocar WorkoutStepNavigator ni services/workout/), logging = UN CompletedSet agregado + guarda isSetEffective en AugeFatigueEngine contra sets cardio (weight=0,reps=0,timeSeconds>0) para evitar fatiga fantasma (~240 "reps" sintéticas), plantillas HIIT como catálogo estático Kotlin (patrón SESSION_TEMPLATES_SYSTEM, cero Room), reorder de bloques con botones ↑↓ (prohibido drag&drop por regresión §23), chart de columnas en vivo siguiendo patrón CalorieTrendChart (NutritionScreen.kt:1089-1195).
§
Plan aprobado pendiente de pipeline (2026-08-20): "Auditoría integral de plantillas + programas avanzados por bloques" en .opencode/plans/2026-08-20_auditoria-plantillas-y-programas-bloques.md — flags: [ios, backend]. **Implementado 2026-08-20**: Fase A (SessionTemplateAuditTest/SplitAuditTest/ProtocolAuditTest + docs/audits/2026-08-20-templates-audit/), Fase B (westside ME/DE, ppl densidad, smolov/bulgarian copy, PHUL/PHAT tags, SESSION_TEMPLATE_PACKAGE_REVISION=v3; TEMPLATE_CATALOG_REVISION permanece v2 del asset), Fase C (BlockGoal/BlockProgressionScheme enums NUEVOS, BlockProgressionEngine/BlockTransitionEngine, ProgramProgressEngine COMPLEX, UI roadmap/banner/hero), Fase D (iOS Program+engines+MacrocycleEditor MVP, backend common.py, docs Room v23). Deuda: export masivo SessionTemplates.swift; tab=/SPLIT huérfanos. Validado: testBaseDebugUnitTest filtrado (audit+engines+catalog+quality+hiit) BUILD SUCCESSFUL.
§
## Catálogo de regresiones (mantenido por el Auditor; el Constructor lo consulta antes de tocar código)

| Fecha | Área / archivo | Patrón de bug | Test que lo detecta | Estado |
|---|---|---|---|---|
| 2026-08-07 | services/workout/ WorkoutVoiceCommandHandler.kt | Doble disparo de save/finalización de sesión de voz | testBaseDebugUnitTest filtrado (voz) | cerrado |
| 2026-08-06 | screens/sessioneditor/ DragController | Bounds congelados por virtualización de LazyColumn en drag&drop | SessionEditor tests dirigidos | cerrado |
| 2026-08-07 | screens/sessioneditor/ EditorMiniField + Sliders | Commits fantasma de AUGE por onChange sin onFinish | SessionEditorRulesEngineTest | cerrado |
| 2026-08-08 | domain/workout/ SupersetRules.normalizeSession | isOptional perdido al normalizar supersets | SupersetRules tests | cerrado |
| 2026-08-09 | domain/workout/ UnilateralRules.kt | Contaminación de drops/rest-pause L↔R en sets unilaterales | UnilateralRules tests | cerrado |
| 2026-08-11 | FoodLoggerDrawer / FoodTemplateMatcher | Regex no posesiva → StackOverflowError al pulsar "Analizar" | Compile + tests nutrición | cerrado |
| 2026-08-18 | screens/home/ HomeViewModel.kt (onboardingState) | Overlay de bienvenida visible antes de que Settings carguen desde Room → flash en cada arranque para usuarios con onboarding completado | OnboardingStateDerivationTest | cerrado |
| 2026-08-18 | screens/home/components/ WelcomeOnboardingOverlay.kt | Scrim sin pointerInput (toques atraviesan a Home/NavigationBar durante la bienvenida) + nameSaved local se pierde al volver del wizard | OnboardingStateDerivationTest + manual (scrim/nameSaved) | cerrado |
| 2026-08-19 | data/models/CardioHiitTemplates.kt | toDetails repite WARMUP/COOLDOWN por intervalRounds (Tabata 68 min vs 12 min) | CardioHiitTemplatesTest (Tabata 12 min) | cerrado (fix 2026-08-19: warmup/cooldown fuera de repeat, intervalRounds=1) |
| 2026-08-19 | data/models/CardioCatalog.kt | supportsSpeed default true muestra km/h en bici/remo/elíptica | manual (CardioIntervalsEditor) | cerrado (supportsSpeed=false para bici/remo/elíptica) |
| 2026-08-19 | screens/sessioneditor/components/CardioIntervalsEditor.kt | Borrar último bloque deja intervalBlocks=[] con targetDurationSeconds=0 → vivo 00:01 | manual | cerrado (empty→20*60, switch off restaura) |
| 2026-08-19 | screens/workout/WorkoutViewModel.kt | TTS intervalo usa speakFeedbackUpdated (bypass VoiceVerbosity) en vez de speakAnnouncement | manual | cerrado (cambiado a speakAnnouncement) |
| 2026-08-20 | screens/sessioneditor/ ScrollRenderer + DragController | SectionZone "loose" inflada por StrengthAddActions (reportaba bounds del fondo de fuerza) → midpoint asignaba a "loose" pointers sobre grupos intermedios | SessionEditorDragControllerTest.inflatedLooseZone_doesNotStealHitTestFromIntermediateGroup | cerrado (fix 9d43bd05: loose solo desde rects de sueltos / sintético 24px / StrengthAddActions solo sesión vacía) |
| 2026-08-20 | screens/sessioneditor/components/ {Exercise,Group,SupersetGroup}EditorCard.kt | onDragCancel llamaba onDragEnd() → cancelación comiteaba el drop | SessionEditorDragControllerTest.cancelExerciseDrag_doesNotInvokeMoveEvenWithResolvedTarget / cancelPartDrag_doesNotMove | cerrado (fix 9d43bd05: cancel → reset sin commit; OJO: los defaults `onDragCancel = onDragEnd` siguen en las cards — callers nuevos deben pasar cancel explícito) |
| 2026-08-20 | screens/sessioneditor/ DragController + cards | Grab offset relativo al handle (28/32/48dp) sesgaba el puntero 10-30px; miembros no-primeros de superset anclaban al top del bloque | SessionEditorDragControllerTest.supersetBlock_singleBoundsEntry_andNonFirstMemberAnchorsToBlock | cerrado (fix 9d43bd05: puntero en window via handleWindowOrigin; superset = bloque único, handles de miembros eliminados) |
| 2026-08-20 | screens/sessioneditor/ Visuals + ScrollRenderer | Drop indicator con expandVertically dentro de la Column medida inflaba la zona al acertar (mergeBounds monótono) | SessionEditorDragControllerTest (secciones disjuntas) | cerrado (fix 9d43bd05: indicador overlay height 0; mergeBounds eliminada; setPartContentBounds única vía) |
| 2026-09-12 | domain/nutrition/ FoodTemplateMatcher + FoodLoggerDrawer | Una descripción parcial podía recuperar una plantilla completa y añadir alimentos omitidos o negados; analizar menciones primero y exigir compatibilidad bidireccional de identidad, cantidad y preparación | FoodTemplateMatcherTest, FoodTemplateMatcherRobustnessTest, NutritionLoggerReliabilityTest | cerrado; suite final 570/570 |
| 2026-09-12 | domain/nutrition/ TextNormalizer, FoodParser, SubjectivePortionEngine, LastResortSplitter | Coma decimal antes del splitter (0,5 kg → 5000 g), fracciones de taza ignoradas, rodajas/rebanadas sobrescritas por unidad entera; preservar duplicados y preparación por mención, exclusiones locales y masa canónica | NaturalLanguageReliabilityTest, LanguageBindingCorpusTest, IndependentNutritionCorpusTest | cerrado; suite final 570/570 |
| 2026-09-12 | domain/nutrition/ TextNormalizer + FoodParser | Números lexicalizados como «tres leches» se volvían cantidad de leche; protección demasiado amplia de «dos/cuatro quesos» y pérdida de scoop durante limpieza. Conservar el nombre y la unidad; un/dos/medio scoop whey deben retener 30/60/15 g hasta V2 | NaturalLanguageReliabilityTest, ReservedNaturalMealCorpusTest, NutritionQaRegressionTest | cerrado; suite final 570/570 |
| 2026-09-12 | domain/nutrition/ FoodIdentity, FoodIndex, SmartFoodResolver, TagResolution; selector FoodLoggerDrawer | Familia o marca no prueban identidad: pavo→pollo, avena→producto de otra composición, whey→marca, leche sin lactosa→queso/candidatos incompatibles. Validar núcleo del nombre, especie, forma y atributos en ranking y en cada opción; considerar catálogo global compatible | FoodIdentityReliabilityTest, FoodIndexTest, NutritionQaRegressionTest, IndependentNutritionCorpusTest | cerrado; suite final 570/570 |
| 2026-09-12 | domain/nutrition/ NutrientBasis, MacroCalculator, CookingStateResolver, TagResolution, FoodInterpretationV2 | Confundir ración con denominador por100g duplicaba nutrientes (Pan Integral gen133); anunciar cocido conservando perfil crudo. Separar porción y base nutricional, convertir volumen con la misma densidad, conservar fuente y traza de conversión, sin reescribir históricos | MacroCalculatorTest, CookingPortionPrecisionTest, FoodIdentityReliabilityTest, NutritionLoggerReliabilityTest | cerrado; JVM final verde |
| 2026-09-12 | domain/nutrition/ NutritionHeuristicEstimator, NutritionInterpretationBridge, FoodInterpretationV2; Home/NutritionScreen | Composición desconocida quedaba con intervalo cero al declarar gramos; ensalada usaba plato genérico y tortilla asumía trigo. Estimación con supuesto y referencias, duda material de composición y rango de densidad independiente de masa; Unsure conserva incertidumbre y nota visible también al reabrir | NutritionQaRegressionTest, NutritionLoggerReliabilityTest, NutritionDurableSaveTest + QA emulador | cerrado; JVM 570/570 + APK final en emulador |
| 2026-09-12 | FoodLoggerDrawer + TagResolution + FoodInterpretationV2 | LARGE inicial se multiplicaba otra vez; edición manual de macros se perdía al redimensionar; cambiar tamaño de arroz inferido lo convertía en peso explícito y reabría pregunta crudo/cocido. Ancla inmutable de masa/nutrientes, preset coherente y RESOLVED_SUBJECTIVE | NutritionLoggerReliabilityTest, IndependentNutritionCorpusTest + QA emulador | cerrado; JVM 570/570 + APK final en emulador |
| 2026-09-12 | screens/nutrition/components/ FoodLoggerDrawer | Arrastrar para descartar y elegir «Seguir editando» dejaba sheet oculto y overlay bloqueante. Restaurar estado visual conservando descripción, alimentos y posición; deshabilitar descarte durante guardado | QA emulador final: arrastre → diálogo → Seguir editando; dumps conservan borrador | cerrado; APK final en emulador |
| 2026-09-12 | data/repository/ NutritionRepository + NutritionCalibrationRepository; NutritionViewModel + FoodLoggerDrawer | Se publicaba éxito antes de persistir Room y quedaba snapshot diario asíncrono; borrador podía cerrarse con error. Esperar transacción comida+snapshot, conservar borrador ante error y reintentar mismo ID sin duplicación | NutritionDurableSaveTest (fallo Room, publicación, reintento, reapertura) + QA guardado/cold start | cerrado; JVM 570/570 + QA emulador |
| 2026-09-12 | NutritionRepository, NutritionCalibrationRepository, SmartFoodResolver, FoodInterpretationV2 | Aprendizaje desde borrador/Unsure y Olvidar incompleto. Aprender sólo dimensiones confirmadas después de persistir; serializar calibración, no repetir aprendizaje por reintento y borrar asociaciones/hábitos/plantillas pertinentes sin borrar registros | NutritionDurableSaveTest, NutritionLoggerReliabilityTest | cerrado; JVM final verde |
| 2026-09-12 | screens/workout + domain/workout etiquetas vivas | Historial reordenaba en vez de filtrar (`tagged + all`), `getWeightSuggestion` caía a `history.first()` y `tagMultiplier` escalaba carga ajena; `userCreatedTags` moría con `ongoing_workout`. Aislar por UUID, persistir `workout_tags` Room v25, 0 kg sin historial de esa etiqueta | WorkoutTagResolverTest, WorkoutTagIsolationTest, WorkoutTagPersistenceTest, TagProgressionAnalyzerTest, ProfileProgressTest (multi-tag RM) | cerrado |
| 2026-09-13 | data/protocols + ProgramProtocolEngine | Publicar síntesis genérica (intensityMin/Max + pool de accesorios) bajo nombre de un autor tercero. Visible = receta día a día + `SessionCompositionPolicy` + `ProtocolSource`; HIDDEN_UNVERIFIED no se aplica | ProtocolRecipeFidelityTest, ProtocolCompositionContractTest, ProtocolClaimsTest, ProgramProtocolEngineTest.applyProtocol_rejects_hidden_unverified_definitions | vigilado |
| 2026-09-13 | ProgramRepository.buildTransitionContext | `readinessScore = null` deja muerto el gate AUGE y la autorregulación semanal. Producción debe pasar el score de `AugeRecoveryEngine.calculateDailyReadiness` | ProgramAutoregulationEngineTest, ProgramProgressEngineAutoregulationTest, BlockTransitionEngineTest | vigilado |
| 2026-09-13 | CompositionTaxonomy.isIsolation | Tratar `articulationType=AISLADO` como aislamiento absoluto convierte hip thrust (y otros compuestos de patrón) en T3 y rompe H1/H9. AISLADO no anula squat/hinge/hip_extension/push/pull | SessionCompositionPolicyTest.hipThrust_catalog_aislado_stays_compound_for_h1, SessionTemplateCatalogTest.systemTemplatesPassSessionCompositionH1ToH10 | vigilado |
| 2026-09-13 | SessionCompositionPolicy H9 / W2 | Bajar suelos H9 (T1 120s / T2 90s) o aplicar MRV de hipertrofia a ciclos PL/especialización obliga exenciones fuera de matriz 2.2. T1>=180s (240 si >=85%), T2>=120s; W2 MRV no aplica a SBD ni a un solo lift | ProtocolExemptionMatrixTest, SessionCompositionPolicyTest, SessionTemplateCatalogTest.systemTemplatesPassSessionCompositionH1ToH10 | vigilado |
| 2026-09-13 | SessionCompositionPolicy H11 / Sheiko | Un `pctBump` lineal sobre plantilla fija publicó 3×5 @102,5 % 1RM. H11 HARD no exentable: ningún set PERCENT_1RM >100, 3+ reps ≤92 % 1RM; Sheiko se recodifica con tabla #29–#32 o se oculta | SessionCompositionPolicyTest.h11_rejects_triple_plus_over_92_percent_1rm, ProtocolRecipeFidelityTest.no_visible_recipe_prescribes_over_100_percent | vigilado |
| 2026-09-13 | Smolov / Smolov Jr | Transponer los 4 esquemas (4×9@70 … 10×3@85) a semanas distintas en vez de días de cada semana. Los tests deben asertar los 4 esquemas *dentro de una semana* | ProtocolRecipeFidelityTest.smolov_base_week_has_four_distinct_day_schemes | vigilado |
| 2026-09-13 | DayArchetypes + SlotSource | `slot()` default AUTHOR hace que GHR/Pallof/JM de arquetipos KPKN se publiquen como del autor. T2/T3 de arquetipo en protocolo de terceros = `KPKN_DEFAULT` | ProtocolAttributionTest | vigilado |

Onboarding bienvenida (2026-08-18): overlay primera vez en Home (WelcomeOnboardingOverlay.kt, slot homeOnboardingOverlay zIndex 102 en MainActivity). Flags JSON en Settings: onboardingCompleted/onboardingProgramDone/onboardingNutritionDone (sin migracion Room, DB sigue v23). Saludo HomeTopBar ahora usa uiState.greeting/userName (antes hardcodeado Hola/Usuario). Flujo: nombre >=3 chars (vacio alerta, Omitir -> Usuario), tarea 1 crea programa con nombre (sin navegar), tarea 2 navega NutritionWizard; al volver, checks y cierre automatico 1.8s via completeOnboarding. Cerrar = dismiss de sesion (reaparece en proximo arranque). Fixes auditoria (cerrado): onboardingState combina repository.isReady y deriva via funcion pura onboardingStateFrom (show = ready && !onboardingCompleted && !dismissed; anti-flash hasta que Settings carguen de Room); scrim del overlay consume toques (pointerInput) para que no se opere Home/NavigationBar por detras; nameSaved se recupera del estado persistido (LaunchedEffect displayName) al volver del NutritionWizard. Validado OnboardingStateDerivationTest (8 casos) + compileBaseDebugKotlin BUILD SUCCESSFUL.

§
Nutrición en lenguaje natural (2026-09-12): [informe y evidencia de auditoría](../../docs/audits/2026-09-nutrition-reliability/README.md). Política aprobada: inferir porciones habituales visibles y aclarar sólo ambigüedad material, sin exigir gramos por defecto; V2 por mención es la fuente de tarjeta y guardado, sin tercer pipeline. Estado: implementación final, validación JVM y QA del APK corregido cerrados. La dirigida final pasó 91/91 y la suite completa 570/570 en 53 suites; `assembleBaseDebug` también pasó. El corpus independiente quedó 23/23 en identidad, cobertura, cantidad y aclaraciones (14 referencias nutricionales evaluadas, 9 sin oráculo). El QA de emulador cubrió exclusiones, composición/Unsure, redimensionado, rangos, guardado/reapertura y recuperación de arrastre en un perfil sintético; no reclamar preservación del perfil poblado perdido ni cobertura de descripciones arbitrarias.
§
Lección operativa de instalación Android (2026-09-12): en el emulador, `adb install -r` eligió instalación incremental y anunció Success, pero el paquete quedó inaccesible y se observó reinicio del perfil/first-install time; el reemplazo posterior con `adb install --no-incremental -r` restauró la app sin recuperar el perfil anterior. No atribuir este incidente a una migración Room: no se ejecutó borrado, desinstalación explícita ni migración. En la continuación, el APK corregido se instaló con `--no-incremental` después de verificar el respaldo `/tmp/kpkn-nutrition-test-profile-before-unsure-fix-install.tar` (3,614,208 bytes, SHA-256 `ec06844c54ddcd1016e31204f7a8f882cbde5a320fab5c5b9063be62bb8c22e0`). El APK final tiene SHA-256 `cc8302eddb193d2daeb445d32eb768e4174fdc7c04d322faf8e578d6597630da`; se verificó en el emulador y se limpiaron los registros sintéticos temporales, conservando sólo el arroz de prueba original. Para reemplazar un APK usar `--no-incremental` y obtener/verificar un respaldo antes de tocar un perfil poblado. El QA utiliza perfil sintético; no prueba conservación de un perfil poblado durante actualización. Evidencia y límites en el informe enlazado.
