# T-002 / v1 — scoped production seams

**TASK_ID / CONTRACT_VERSION / ROLE:** T-002 / v1 / READER  
**STATUS:** SUBMITTED  
**INPUT_BASELINE / OUTPUT_CANDIDATE:** Contract-supplied HEAD `ac3ff1c88d8843aca986d0b5600351b8ff1303cc`; workspace includes WIP. Candidate: report only. Key candidate SHA-256: `SetupBasicSteps` F6A8625B9F46E6C69F9808E0844A1F801FF52D1869544C281EC0ED779AEA1F96; `WizardPhysiqueSelector` 3B83080D22E9261279763640EDA73F6785AC5967CDE5C4458F2BBF9B19C0A324.  
**ACTUAL_ACTIONS:** Targeted static re-reads of the named muscle, split, and body-fat seams; scoped diff and prior SHA-256 fingerprints retained.  
**ARTIFACTS:** `.opencode/orchestration/wizard-feedback-ses_f2efb28a7ffe-20260925/T-002-report.md`

**ACCEPTANCE_EVIDENCE** *(static evidence only)*
- **AC-1 PASS:** Body-fat/source/persistence seams mapped; no timings measured.
- **AC-2 PASS:** IDs, units, validation/resume/review mapped; fit unmeasured.
- **AC-3 PASS (static mapping evidence):** Literal selectable keys, point constraints, aliases, split ID/display seams, and stale-selection rejection point are now listed below. No points were allocated.
- **AC-4 PASS:** Error owner and WIP boundaries mapped.

**VALIDATION:** Static only; build/test/device/layout/frame checks **NOT RUN**. Future: rapid-drag write trace; 360dp, short-height, 200%-text fit.  
**SCOPE_CHECK:** Report only created; all existing onboarding WIP preserved.  
**LIMITATIONS_AND_RISKS:** Static evidence cannot prove gesture smoothness or combined fit.  
**BLOCKER:** None for this static reader handoff. Point allocation remains the user's decision; no allocation or new key was introduced. Runtime fit/performance remain unmeasured.

**DETAILED_REPORT**

- **Body fat:** `SetupBasicSteps.kt:241-405` requires source choice before the selector (direct-entry mismatch). Figure/position do not affect equation sex (`design/WizardPhysiqueSelector.kt:108-145,169-214`; equation `SetupBasicSteps.kt:196-227`); activation keeps measured/estimated provenance and omits unknown (`SetupActivationPayload.kt:58-80`). Press/drag emits repeated callbacks (`WizardPhysiqueFrames.kt:48-81`); production writes position and percent/source through serialized VM saves (`SetupBasicSteps.kt:389-404`; `SetupWizardViewModel.kt:883-902`). This is a risk, not a measurement. `painterResource` switches frames; repeated decode is unproven (`WizardPhysiqueSelector.kt:180-197`). Measure writes, save duration, recompositions, frame timing. Tests: `WizardGateComponentsUiTest.kt:358-437`; unknown journey `SetupWizardFullJourneyUiTest.kt:294-299`.
- **Height/weight:** Stable sequential IDs and cm/kg values, with independent cm/ft-in and kg/lb units (`SetupStepGraph.kt:34-67,308-318`; `SetupWizardModels.kt:217-222,287-296`). Validation/resume/review links remain separate (`SetupWizardModels.kt:839-863`; `SetupDraftCompatibility.kt:34-40,241-290`; `SetupReviewStep.kt:161-179`). Controls are 5×44dp vertical height vs 110dp horizontal weight (`WizardHeightWheel.kt:47-49`; `WizardWeightRule.kt:50-53`). Scaffold exposes height but scrolls overflow (`WizardScaffold.kt:107-163`); merged UI needs a new horizontal height ruler and has no fit evidence. Keep IDs/data. Tests: `WizardMeasuresTest.kt:19-152`; `WizardControlSemanticsUiTest.kt:42-135`; no 360dp/200%-text test found.
- **Split/muscles/errors/WIP:** English IDs/Spanish copy (`data/splits/SplitTemplates.kt:38-50,139-155`); static recommendation first, compatible cards after, no search/carousel (`SetupTrainingSteps.kt:117-123,373-396`). Stale IDs pass answered validation (`SetupWizardModels.kt:944-947`). Current muscle question is “¿Qué músculos priorizas?”; `Todo el cuerpo` maps to `FULL_BODY`/empty focus targets (`SetupStepDefinitions.kt:471-478`; `SetupWizardModels.kt:149`; `SimpleCyclePersonalizer.kt:514-522`). Spanish order keys feed ordering-only engine (`SetupStepDefinitions.kt:187-191`; `SimpleCyclePersonalizer.kt:525-557`). Safe groups: glute max/medius→`Glúteos`, hamstrings→`Isquiosurales`, wide-back→`Dorsales`; dense-back unresolved (`CompositionTaxonomy.kt:131-135`; `SimpleCyclePersonalizer.kt:727-744`). Host shows all error keys above the step; validation is submit-triggered and save/preview errors share the banner (`SetupWizardHost.kt:112-125,158-204`; `SetupWizardViewModel.kt:526-531,853-913`); generation details: T-001. `#1F1F1F`/fixed CTA/insets remain (`WizardDesignTokens.kt:62-64`; `WizardScaffold.kt:267-300`). Preserve welcome/visual-gate WIP.

## v2 correction evidence (targeted static re-read)

- **Literal priority keys/consumer:** `ORDER_MUSCLE_OPTIONS` exposes exactly `Pectorales`, `Dorsales`, `Deltoides`, `Bíceps`, `Tríceps`, `Cuádriceps`, `Isquiosurales`, `Glúteos`, `Pantorrillas`, `Abdomen`, `Trapecio`, `Erectores Espinales` (value equals label), `SetupStepDefinitions.kt:187-191`. `orderPointsFromBag` normalizes keys, merges aliases that collapse, rejects negatives and >2 per normalized muscle, and rejects total >5; `<=5` is valid, points need not total five (`SimpleCyclePersonalizer.kt:700-719`; runtime scoring `533-557`). `canonicalSelection` explicitly maps singular `trapecio`→`Trapecio`, but not plural `Trapecios`; plural falls through to `VolumeCalculator.normalizeCanonicalMuscleGroup` (`727-744`), outside this targeted read, so its support is unverified. `CompositionTaxonomy` maps `trapezius`/`rhomboids` to `BACK_UPPER` (`CompositionTaxonomy.kt:129-136`), but the inspected ordering seam exposes no `BACK_UPPER` selectable key or explicit alias; do not treat it as supported. Glute max and medius share taxonomy `GLUTES` (`135`) and the one selectable `Glúteos` key; emit that normalized key once. If duplicate aliases are supplied, the normalizer sums their points and enforces the two-point cap (`SimpleCyclePersonalizer.kt:708-716`).
- **Split seam/compatibility:** `SplitTemplate(id, name, description, tags, pattern, difficulty, pros, cons, sessionDescriptions, publicationStatus, dayDefinitions)` is the display/model boundary (`data/splits/SplitTemplates.kt:38-50`); stable IDs include `ul_x4`, `fullbody_x3`, `glute_focus`, `ppl_x3`. English/mixed display names needing Spanish display-only labels include “Upper / Lower x4”, “Full Body x3”, “Push / Pull / Legs x3”; keep IDs unchanged. “Especialización Glúteo” is also a catalog example (`141,143,155,300`). `compatibleSplitTemplates(daysPerWeek,startDay)` filters visibility and derived day count; `TrainingSplitStep` calls it and renders `split.name` + `pattern`, not `description` (`SetupTrainingSteps.kt:117-123,368-385`). Wizard validation only checks answered (`SetupWizardModels.kt:944-947`); `SimpleCyclePersonalizer.resolveSplitPlan` rejects missing/hidden IDs and day-count mismatch (`585-615`). Keep this engine check for stale IDs; current recommendation card/rationale is static (`SetupTrainingSteps.kt:373-377`).
- **Body-fat write seam:** UI routes each slider callback to `updateStep(BODY_FAT) { copy(physiqueSliderPosition=…) }` and separately `onCandidateChange` calls `setStepNumber` then `setStepChoice(VISUAL_ESTIMATE)` (`SetupBasicSteps.kt:389-404`). VM exposes `setStepNumber`, `setStepChoice`, and generic `updateStep`, each routed through `mutateDraft` (`SetupWizardViewModel.kt:185-187,221-231,883-895`). There is no body-fat-specific atomic setter; generic `updateStep` can combine fields in one mutation/save. `VerticalPhysiqueSlider` exposes only `onPosChange`: drag handler has no finish/cancel callback, and a second pointer handler emits on press (`WizardPhysiqueFrames.kt:48-81`). The visual-only selector also confirms in its position callback (`WizardPhysiqueSelector.kt:198-208`). No final-value-only UI callback exists today; adding one is the smallest gesture seam. This is static call-path evidence, not a write/frame measurement.
