---
description: ejecutar un plan KPKN aprobado con Constructor
agent: constructor_kpkn
---

Lee y ejecuta el plan aprobado indicado abajo. Comprueba `pipeline` y no edites si no está en `construction`; usa `resume_construction` para una corrección pendiente. Usa `kpkn_map` y `memory` antes de explorar ampliamente, delega trabajo acotado a `mano-extra` cuando ahorre contexto, valida con tests/build, llama `submit_audit` y deja los documentos generados actualizados.

Plan o ruta del plan:
$ARGUMENTS
