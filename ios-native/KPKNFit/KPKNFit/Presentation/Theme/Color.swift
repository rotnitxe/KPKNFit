import SwiftUI

extension Color {
    // ─── Light Palette ──────────────────────────────────────────────────────
    static let purple40 = Color(hex: 0x6750A4)
    static let purpleGrey40 = Color(hex: 0x625B71)
    static let pink40 = Color(hex: 0x7D5260)

    // ─── Dark Palette (OLED optimized) ──────────────────────────────────────
    static let darkBackground = Color(hex: 0x0F0F0F)
    static let darkSurface = Color(hex: 0x1A1A1A)
    static let darkSurfaceVariant = Color(hex: 0x252525)

    static let primaryDark = Color(hex: 0xD0BCFF)
    static let secondaryDark = Color(hex: 0xCCC2DC)
    static let tertiaryDark = Color(hex: 0xEFB8C8)

    // ─── Ring Colors ────────────────────────────────────────────────────────
    static let ringRed = Color(hex: 0xFF5252)
    static let ringBlue = Color(hex: 0x448AFF)
    static let ringYellow = Color(hex: 0xFFD740)
}
